// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyCLSyAK_mDpXZpZLP5zgGpsWy6vmzp40BY",
    authDomain: "computers-store.firebaseapp.com",
    databaseURL: "https://computers-store.firebaseio.com",
    projectId: "computers-store",
    storageBucket: "computers-store.appspot.com",
    messagingSenderId: "700700639592",
    appId: "1:700700639592:web:e5ce9d4be5cf91ef"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
